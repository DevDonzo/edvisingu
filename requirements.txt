# EdVisingU — OFFLINE CORE ($0, no API keys).
# Everything needed to run the full demo, CLI, and 59-test suite:
#   pip install -r requirements.txt
#
# Versions are floored at the builds proven on Python 3.14 (older pins like
# pydantic 2.9 have no 3.14 wheel and fail to build). Every paid SDK is
# lazy-imported behind ARCH_BACKEND=live + a key, so it lives in
# requirements-live.txt and is only needed when going live on the VPS.
fastapi>=0.137.1
uvicorn>=0.49.0
pydantic>=2.13.4
httpx>=0.28.1
python-dotenv>=1.2.2
pytest>=9.1.0
